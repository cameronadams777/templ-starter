package components

type FlashMessageProps struct {
	Message string
}

templ FlashMessage(props FlashMessageProps) {
	if props.Message != "" {
		<div id="flash-message" class="bg-red-500 text-white p-4">
			{ props.Message }
		</div>
	} else {
		<div id="flash-message"></div>
	}
}
